"""Status-only Telegram adapter primitives for the Stateful VM runtime.

This package is wired into Terraform and systemd behind an explicit disabled-by
default enable flag. It provides fixed command handling for status-only
Telegram operator reads.
"""
